<template>
    <div class="page" style="flex-direction: column;">
        <list class="list">
            <list-item class="item" @click="routerTo('pageSizeSetting')" type="item">
                <div class="item-content">
                    <text class="itemtext" static>分段大小</text>
                    <text class="itemtext2" static>一次性加载的字节</text>
                </div>
                <img src="/common/images/enter.png" class="arrow" static/>
            </list-item>
            <list-item class="item" @click="togglePreventParagraphSplitting" type="item" if="{{readMode !== 'nostalgic'}}">
                <div class="item-content">
                    <text class="itemtext" static>段落防分割</text>
                    <text class="itemtext2" static>避免分段截断段落</text>
                </div>
                <img src="{{preventParagraphSplitting ? '/common/images/Switch_ON.png' : '/common/images/Switch_OFF.png'}}" class="switch"/>
            </list-item>
            <list-item class="item" @click="toggleReadingTimeRecording" type="item">
                <div class="item-content">
                    <text class="itemtext" static>阅读时长记录</text>
                    <text class="itemtext2" static>记录阅读时长统计</text>
                </div>
                <img src="{{readingTimeRecording ? '/common/images/Switch_ON.png' : '/common/images/Switch_OFF.png'}}" class="switch"/>
            </list-item>
            <list-item class="item" @click="routerTo('progressSavingSetting')" type="item">
                <div class="item-content">
                    <text class="itemtext" static>阅读进度保存</text>
                    <text class="itemtext2" static>设置进度保存方式</text>
                </div>
                <img src="/common/images/enter.png" class="arrow" static/>
            </list-item>
            <list-item class="item" @click="routerTo('marqueeSetting')" type="item">
                <div class="item-content">
                    <text class="itemtext" static>跑马灯设置</text>
                    <text class="itemtext2" static>各页面跑马灯开关</text>
                </div>
                <img src="/common/images/enter.png" class="arrow" static/>
            </list-item>
            <list-item class="item" style="padding: 7px 16px;" type="tip">
                <text class="itemtext2" style="lines: 3;">调整前猜一猜，为什么这里叫"性能设置"。</text>
            </list-item>
        </list>

        <img static src="/common/images/hd.png" style="position: absolute;left: 0px;top: 0px;width: 336px;height: 102px;" />
        <img static src="/common/images/back.png" @click="back" style="position: absolute;left: 6px;top: 6px;width: 72px;height: 72px;" />
        <text style="position: absolute;left: 78px;top: 7px;width: 180px;line-height: 32px;font-weight:bold;font-size:24px;color:rgba(255,255,255,0.6);text-align:center;">
            {{nowTime}}
        </text>
        <text static style="position: absolute;left: 78px;top: 35px;width: 180px;line-height: 42px;font-weight:bold;font-size:32px;color:white;text-align:center;">
            性能设置
        </text>
    </div>
</template>

<script>
    import router from '@system.router'
    import storage from '../../utils/storage.js'

    export default {
        private: {
            nowTime: "00:00",
            timer: null,
            timeFormat: '24h',
            preventParagraphSplitting: false,
            readingTimeRecording: false,
            readMode: 'scroll'
        },
        
        onInit() {
            storage.get({
                key: 'EBOOK_TIME_FORMAT',
                success: (data) => {
                    if (data) this.timeFormat = data;
                    this.updateTime();
                },
                fail: () => {
                    this.updateTime();
                }
            });
            
            this.timer = setInterval(() => {
                this.updateTime();
            }, 60000); 
            
            storage.get({
                key: 'EBOOK_PREVENT_PARAGRAPH_SPLITTING',
                success: (data) => {
                    if (data) this.preventParagraphSplitting = data === 'true';
                },
                fail: () => {
                    this.preventParagraphSplitting = false;
                }
            });
            
            storage.get({
                key: 'EBOOK_READING_TIME_RECORDING',
                success: (data) => {
                    if (data !== undefined && data !== '') {
                        this.readingTimeRecording = data === 'true';
                    } else {
                        this.readingTimeRecording = false;
                    }
                },
                fail: () => {
                    this.readingTimeRecording = false;
                }
            });

            storage.get({
                key: 'EBOOK_READ_MODE',
                success: (data) => {
                    if (data) this.readMode = data;
                }
            });
        },
        
        onDestroy() {
            if (this.timer) {
                clearInterval(this.timer);
                this.timer = null;
            }
        },
        
        updateTime() {
            const date = new Date();
            let hours = date.getHours();
            let minutes = date.getMinutes();

            if (this.timeFormat === '12h') {
                let ampm = hours >= 12 ? '下午' : '上午';
                hours = hours % 12;
                hours = hours ? hours : 12;
                minutes = minutes < 10 ? '0' + minutes : minutes;
                this.nowTime = `${ampm} ${hours}:${minutes}`;
            } else {
                hours = hours < 10 ? '0' + hours : hours;
                minutes = minutes < 10 ? '0' + minutes : minutes;
                this.nowTime = `${hours}:${minutes}`;
            }
        },
        
        routerTo(url) {
            router.push({
                uri: '/pages/' + url
            });
        },
        
        togglePreventParagraphSplitting() {
            this.preventParagraphSplitting = !this.preventParagraphSplitting;
            storage.set({
                key: 'EBOOK_PREVENT_PARAGRAPH_SPLITTING',
                value: this.preventParagraphSplitting.toString()
            });
        },
        
        toggleReadingTimeRecording() {
            this.readingTimeRecording = !this.readingTimeRecording;
            storage.set({
                key: 'EBOOK_READING_TIME_RECORDING',
                value: this.readingTimeRecording.toString()
            });

            if (this.readingTimeRecording) {
                router.push({
                    uri: '/pages/help',
                    params: {
                        title: '阅读时长记录提醒',
                        content: '阅读时长记录可能会造成严重卡顿，且不接受反馈。在校学生不建议打开。'
                    }
                });
            }
        },
        
        back() {
            router.back();
        }
    }
</script>

<style>
  @import '../../common/style.css';
</style>
